---
- name: localhost
  tasks:
    - name: playbooks
        - include: "{{item}}"
          with_items:
            - image.yml
            - flavor.yml
            - keypair.yml
            - volume.yml
            - project.yml
            - instance.yml
        
