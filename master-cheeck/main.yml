---
- name: localhost
  tasks:
    - name: install Openstack Client package
      roles:
        - main
    - name: playbooks
        - include: "{{item}}"
          with_items:
            - image.yml
            - flavor.yml
            - keypair.yml
            - volume.yml
            - project.yml
            - instance.yml
        
